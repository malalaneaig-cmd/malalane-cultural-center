node_modules
dist
.DS_Store
*.log
.env
.env.local
.env.*.local
~$*
.vercel

# Local IDE/tool config (may contain personal MCP tokens/keys)
.cursor/

# Raw working documents and unprocessed media (not part of the built site).
# Keep these in Drive/SharePoint or an asset bucket, not in git history.
CONTENT FOR MCC WEBSITE/
MCC TEMPORARILY GINE OFFSITE/
PUBLIC PHOTOS/
PUBLIC VIDEOS/

# Sensitive credentials — never commit (Stripe 2FA backup codes, etc.)
STRIPE CODE/
